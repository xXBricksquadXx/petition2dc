# Server
PORT=3000
BASE_URL=http://localhost:3000
SESSION_SECRET=change-me-in-prod

# SQLite database file path (relative ok)
DB_PATH=./data/app.sqlite

# Admin login (generate via: node scripts/hash-password.mjs "your password")
ADMIN_PASSWORD_SALT_HEX=
ADMIN_PASSWORD_HASH_HEX=

# Optional: trust reverse proxy (set true on many PaaS / Docker behind proxy)
TRUST_PROXY=false
